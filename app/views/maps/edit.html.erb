<%= form_for @map do |form| %>
	<fieldset>
	    <legend>Edit Minecraft Map</legend>
		<%= render "maps/form", form: form %>
		<%= link_to "GoTo Maps", maps_path, class: "btn" %>
		<%= form.submit "Update Map", class: "btn btn-primary" %>
	</fieldset>
<% end %>